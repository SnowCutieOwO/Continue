# 结构（Structure）

表示通过 ID 对结构的引用。

## 用途

只有一个参数用到：

* FEATURE 中的 base：

  <badge type="info" text="structure.structures" /> [权重列表](config-packs.config-documentation.config-objects.weightedlist.md)<[结构](config-packs.config-documentation.config-objects.structure.md)>
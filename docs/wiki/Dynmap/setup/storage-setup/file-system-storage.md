# 文件存储系统

这个选项是 Dynmap 的默认存储选择。它的结构最简单，也有最明显的缺点：渲染时会创建成千上万的文件，每个文件都占用几个 KB。

生成的文件存储在 `dynmap/web/tiles/<世界名称>/<前缀>` 文件夹中。`<前缀>` 则是通过 `worlds.txt` 或“角度”参数指定的图块存储前缀。在文件夹中你可能会找到“z”开头的文件，这些是“缩放图块”，会在每个不同的缩放等级下显示，“z”的数量则代表了它们对应的缩放等级。每个缩放等级与其上下级成两倍关系，即下一级缩放是上一级的四分之一大小。